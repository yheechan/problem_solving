#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char **argv) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int N, K;
  cin >> N >> K;

  vector<int> JOSEPHUS(N);
  for (int i=0; i<N; i++) {

  }

  return 0;
}
